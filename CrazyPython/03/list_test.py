# 列表
a_tuple = ("crazy", 1,2, "hello")
# 将元祖转换为列表
a_list = list(a_tuple)
print(a_list)

# 使用range函数创建区间对象
a_range = range(1, 5)
# 将区间转化为列表
b_list = list(a_range)
print(b_list)

# 创建区间的时候指定步长
c_list = list(range(1, 10, 2))
print(c_list)






